var expect = require('expect');
var ItalicKey = require('../ItalicKey');

var content = {
};
var selection = {
};

describe('ItalicKey', () => {
  describe('#matches', () => {
    it('matches event', () => {
    });
    it("doesn't match event", () => {
    });
  });

  describe('#execute', () => {
    it('modifies the selected content', () => {
    });
  });
});
