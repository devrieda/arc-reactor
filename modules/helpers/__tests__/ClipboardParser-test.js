import ClipboardParser from '../ClipboardParser';

describe('ClipboardParser', function() {

  describe.skip('#parse', function() {
    it('does something', function() {
      const pasted = "";
      const content = {};
      const selection = {};
      const callback = Function.prototype;
      ClipboardParser.parse(pasted, content, selection, callback);
    });
  });
});
