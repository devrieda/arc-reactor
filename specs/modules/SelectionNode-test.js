var assert = require('assert');
var expect = require('expect');
var React = require('react');

var SelectionNode = require('../../lib/modules/SelectionNode');

describe('SelectionNode', function() {
  it('create a new selection', function() {
  });
});
